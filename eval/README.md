# eval（里程碑5）

Agent 的工程严谨度核心：可度量、可回归。计划：

- **数据集**：`(用户输入 → 期望抽取字段 / 期望工具调用 / 期望最终动作)` 标注样本。
- **指标**：字段抽取 F1、工具调用正确率、任务成功率（LLM-as-judge）、平均轮次、token/延迟/成本。
- **回归**：golden 样本进 CI，prompt/模型变更前后对比，防止"改一处崩一片"。
- **追踪**：LangSmith / Langfuse 记录每次 run 的节点路径与工具调用。

（占位，里程碑1–4 完成后填充。）
