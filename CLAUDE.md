# CLAUDE.md

本仓库对 Claude Code 的协作约定。**新会话请先读本文，再读 `docs/HANDOFF.md`（上下文/进度/下一步），规格见 `docs/PRD.md`。**

## 这个项目是什么（一句话）

为求 **AI/Agent 开发岗**，把作者在 xTool 上线（51 名工程师在用）的机械部自采系统的"大脑"，用 **Python + LangGraph** 重写为一个独立、可展示、作者完全掌控的**采购受理 Agent**。北极星 = **父 Orchestrator + 角色化 Subagent**。**最看重工程严谨度（eval / 可观测 / HITL / 持久化 / 护栏），不是堆功能。**

## 原则（第一性原理）

从原始需求和问题本质出发，不从惯例或模板出发。
1. 不假设作者清楚自己想要什么；动机/目标不清晰时**停下来讨论**。
2. 目标清晰但路径不是最短的，**直接说并建议更好的办法**。
3. 遇到问题**追根因，不打补丁**；每个决策都能回答"为什么"。
4. 输出**说重点**，砍掉一切不改变决策的信息。

## 行为准则

1. **先思考再编码**：写出假设；多种解读都摆出来；有更简单方案就说、该反对就反对；不清楚就停下问。
2. **优先简单**：最少代码解决问题；不写投机性/没要求的功能；不过度设计。资深工程师会觉得过度设计的，就简化。
3. **外科式修改**：只动必须动的；不顺手优化相邻代码；匹配既有风格；清理自己改动产生的孤儿（import/变量），但不删原本就有的死代码（除非作者要求）。
4. **目标驱动**：先定可验证的验收标准，再循环到验证通过。
5. **不 vibe-coding**：作者要逐块理解。写代码时**逐块解释 是什么/为什么/怎么办，并附该块对应的大厂面试题**。

## 沟通约定

- 一律中文（解释、状态、提问、commit message）。
- 代码标识符、技术术语、外部 API 名保持英文原貌。

## 技术栈（已定）

Python 3.11+ · LangGraph · langchain-openai（OpenAI 兼容端点，默认 DeepSeek：`deepseek-chat` 快 / `deepseek-reasoner` 深）· Pydantic · PostgreSQL(checkpointer，后期) · LangSmith(可观测/eval，后期) · uv/pip · ruff/pytest。
环境：Windows + cmd.exe（**注意 `#` 不是注释符**）；`.venv` 已建；`pip install -e .` 可用；`.env` 配 LLM key。
